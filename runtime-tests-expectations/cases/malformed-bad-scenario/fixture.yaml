# Fixture: malformed — a scenario uses an unknown scenario value (BUILD/PUBLISHED/COMPAT).
# Expected: RuntimeTestDescriptors.parseScenario() throws ValidationError(... "unknown scenario").
mod_id: terrasect
latest: "26.2"
mod_version: "0.2.3"

scenarios:
  - mc: "26.2"
    loader: fabric
    scenario: FOO
    gameVersions: ["26.2"]
    dependencies: []
