# Fixture (part 2 of header-mismatch): this manifest's header (mod_version 0.2.3) differs from the
# first manifest in the directory, so validate()'s header-equality check flags it as "mismatch".

mod_id: terrasect
latest: "26.2"
mod_version: "0.2.3"

scenarios:
  - mc: "26.2"
    loader: fabric
    scenario: BUILD
    gameVersions: ["26.2"]
    dependencies: []
