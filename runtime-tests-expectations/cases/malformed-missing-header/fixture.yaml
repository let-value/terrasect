# Fixture: malformed — required top-level header `mod_id` is absent.
# Expected: RuntimeTestDescriptors.parse() throws ValidationError("missing mod_id in fixture.yaml")
latest: "26.2"
mod_version: "0.2.3"

scenarios:
  - mc: "26.2"
    loader: fabric
    scenario: BUILD
    gameVersions: ["26.2"]
    dependencies: []
