# TODO

Scratchpad for the orchestrator. One line per active external-agent item.

**How to process an item:**
1. Pick an item from the list below.
2. Create a goal file for it in `docs/goals/` following the template in `docs/KNOWLEDGE_BASE.md`.
3. Create or reuse the task's dedicated workspace/worktree before delegating.
4. Invoke the selected provider with: "Read `docs/goals/GOAL_[name].md` and write your response there."
5. Verify the goal file `Status` is `COMPLETED` and `## Response` is populated.
6. Delete the line from this file.

Do not mark items done here — just delete them when the goal is completed. Completed work is summarized in `docs/goals/README.md`.

---

<!-- Add active tasks below this line -->
- loot-generation-manipulation investigation: /home/alex/terrasect/.worktrees/loot-generation-manipulation/docs/goals/GOAL_20260620_065520_Loot_Generation_Manipulation_Investigation.md
