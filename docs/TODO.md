# TODO

Scratchpad for the orchestrator. One line per active item.

**How to process an item:**
1. Pick an item from the list below.
2. Create a goal file for it in `docs/goals/` following the template in `docs/KNOWLEDGE_BASE.md`.
3. Invoke the selected provider with: "Read `docs/goals/GOAL_[name].md` and write your response there."
4. Verify the goal file `Status` is `COMPLETED` and `## Response` is populated.
5. Delete the line from this file.

Do not mark items done here — just delete them when the goal is completed. Completed work is summarized in `docs/goals/README.md`.

---

- Update `docs/PROJECT_MAP.md` with explicit architecture separation and a grounded explanation of SDF usage for region boundaries. Before writing, inspect the actual code under `/home/alex/terrasect/common/`, `/home/alex/terrasect/fabric/`, `/home/alex/terrasect/neoforge/`, and `/home/alex/terrasect/versions/`; correct or remove unsupported claims from the first project-map draft.
