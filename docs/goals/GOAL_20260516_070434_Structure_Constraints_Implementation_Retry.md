Status: READY TO MERGE on branch `structure-constraint-statistics-instrumentation`, PR #54, worktree `/home/alex/terrasect-worktrees/structure-constraint-statistics-instrumentation` — The structure-constraints topic is consolidated here from the original implementation/retry goal and the duplicate structure-statistics instrumentation follow-up: prior phases researched Minecraft structure placement internals, implemented shared structure constraints (`StructureConstraints`, `StructureHandler`, `CompiledStructureLookup`, `DimensionContext`, locate/generation filtering, placement overrides, and selection/tag semantics), added deterministic client GameTests and live screenshot/locate/statistics coverage, then PR #54 rewrote `StructureConstraintStatisticsGameTest` to validate generated structures through instrumentation instead of expensive locate calls, added per-event instrumentation filtering, recorded both placed chunk and logical `origin_chunk` from `StructureStart.chunkPos`, deduplicated generated structures by `structure_id|origin_chunk` while filtering by placed chunks in the 10x10 area, fixed stale zero-valued counter bleed-through from `InMemoryBackend.reset()`, banned explicit village variants plus tag coverage, removed the `TERRASECT_STRUCTURE_ARGS` thread-local by using a direct `@WrapOperation` with `chunkAccess`/`dimensionKey` locals, kept direct `ChunkAccessExtender.terrasect$getContext()` with dimension-key fallback for chunks missing attached context, and refreshed deterministic snapshots so vanilla/banned-village each report four mineshafts while dense reports 16 structures including three villages; verification recorded for this final state: `./gradlew :common:compileJava :common:compileKotlin --no-daemon`, `./gradlew :fabric:compileGametestKotlin :common:compileKotlin`, `./gradlew :fabric:runClientGameTest -Ptest=StructureConstraintStatisticsTest -PupdateSnapshots=true --no-daemon`, `./gradlew :fabric:runClientGameTest -Ptest=StructureConstraintStatisticsTest`, `./gradlew spotlessCheck --no-daemon`, and `git diff --check` all passed, with final banned-village evidence `structure.applied=1024`, `target_village_event_values=0`, `total=4`, and only mineshaft rows in the snapshot; next step is merge PR #54 after this goal-file compaction/PR-body refresh commit is pushed.
