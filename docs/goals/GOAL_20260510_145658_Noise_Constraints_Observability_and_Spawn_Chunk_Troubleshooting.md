Status: IN REVIEW (branch noise-narrative-constraints, PR #50, https://github.com/let-value/terrasect/pull/50) — Added SLF4J observability to CompiledNoiseConstraints, DimensionContext, and NoiseHandler (commit 4915b9c) plus a SpawnChunkNoiseConstraintTest; discovered that wrapping top-level NoiseRouter fields after mapAll is insufficient for noise→climate routing and fixed it by wrapping the NoiseRouter argument passed into NoiseChunk.cachedClimateSampler so noise-only scenarios drive biome selection through the native path without ClimateHandler; removed ThreadLocal hot-path plumbing in favour of creation-time NoiseChunk binding (scoped ThreadLocal + keyed-holder wrapping in NoiseChunkFunctionsMixin); replaced a NoiseDebug opt-in flag with SLF4J namespace logging (NoiseScope.kt, dot-separated noise/noise.registry/noise.handler/noise.handler.origin.* hierarchy); expanded NoiseNarrativeConstraintTest to eight scenarios (vanilla, desert, ocean, highlands, hilly_plains, flat_plains, lowlands, ridge_valley) with flying aerial camera at y=160; final verified evidence: desert surface=70-74 sand×246 biomes=[desert×256], ocean surface=63-67 water×225 biomes=[deep_ocean×256], ridge_valley intentional water corridor biomes=[swamp×256]; Claude hit error_max_turns several times and Hermes resumed from worktree each time; multiple review cleanup passes addressed mixin thinness, handler-owned logging, ScopedLogger traceBlock/debugBlock API, and scenario determinism; Java available via `JAVA_HOME=/home/alex/.local/share/mise/installs/java/21`.
