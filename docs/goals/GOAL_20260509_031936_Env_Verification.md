Status: COMPLETED — Verified `/home/alex/terrasect` Java/Kotlin/Gradle readiness: build files use Java 21, Kotlin 2.3.0, Gradle 9.3.0, Spotless, and `-PupdateSnapshots`; Java was not originally on PATH so the working invocation was `mise exec java@21 -- bash ./gradlew` / `bash ./gradlew` rather than direct wrapper execution with unset `JAVA_HOME`; `:fabric:compileJava` and `:fabric:test` passed with all game tests.
