Status: COMPLETED — Verified the local Java/Kotlin/Gradle setup for /home/alex/terrasect; build files confirmed Java 21, Kotlin 2.3.0, Gradle 9.3.0, Spotless formatting, and `-PupdateSnapshots` test flag support; Java was absent from PATH so the correct invocation is `mise exec java@21 -- bash ./gradlew`; `bash ./gradlew` is required over direct wrapper execution because JAVA_HOME is unset in the shell; `:fabric:compileJava` and `:fabric:test` both completed successfully with all game tests passing.
