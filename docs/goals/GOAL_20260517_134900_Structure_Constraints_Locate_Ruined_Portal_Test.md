# Goal: Add a locate GameTest for ruined portals

This locate test added a ruined-portal GameTest alongside the existing village locate coverage by refactoring the shared locate helper, asserting that the live locate result resolves to a ruined-portal structure at the fixed deterministic distance, and capturing the screenshot artifact after the live teleport.
