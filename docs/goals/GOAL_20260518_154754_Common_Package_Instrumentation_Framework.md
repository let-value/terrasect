Status: COMPLETE on branch `feature/common-instrumentation`, worktree `/home/alex/terrasect/.worktrees/common-instrumentation` — The common instrumentation topic added a disabled-by-default scoped instrumentation API under `common/src/main/kotlin/terrasect/instrumentation/` (`InstrScope`, `MetricEvent`, `MetricTag`, scope-aware IDs, snapshots, `MetricsConfig`, global `Instr`, `ScopedInstr`, counters/timers, lazy zero-to-three-tag overloads, and tests), integrated semantic counters/timers into structure/noise/climate paths through curated `TerrasectInstr` scopes/events, then followed commits `ae9ee8c` and `fbd3655` with code-quality cleanup that hoisted reusable handles, made them immutable, standardized logger fields to `log`, removed stale reset/debug scaffolding and redundant instrumentation locals, cleaned GameTest log/assertion text, and verified with Spotless plus `:common:compileKotlin`, targeted `MetricsTest`, `:fabric:compileKotlin`, `:fabric:compileGametestKotlin`, `:neoforge:compileKotlin`, `git diff --check`, and repository searches for stale `LOGGER`/mutable `instr`/redundant `TerrasectInstr`/`resetOriginTrace` artifacts.
