Status: COMPLETED — Investigated the crash in worktree noise-narrative-constraints at fabric/build/run/clientGameTest/ and confirmed the hypothesis: the crash is exclusively caused by running `runClientGameTest` in a headless environment because GLFW raises GLFW_PLATFORM_UNAVAILABLE (0x1000E) inside Minecraft.<init> before any game logic runs; Terrasect mod initialization completed successfully before the crash, ruling out mod code bugs, missing dependencies, LWJGL version mismatches, and GPU faults; fix is to run the task inside an Xvfb virtual display session (e.g., `Xvfb :99 -screen 0 1280x1024x24 & DISPLAY=:99 ./gradlew :fabric:test`); no Terrasect code changes are required.
