<h2>Two-region demo</h2>
<p>Same clamp rules, but applied per-region with edge blending (8-block boundary zone).</p>
<table><tr><th>Scenario</th><th>Values</th><th>Ocean Mask</th><th>Region Map</th><th>Interior Stats</th></tr>
<tr><td><code>NO_OCEAN</code> vs <code>ONLY_OCEAN</code></td><td><img width="{{imgSize}}" height="{{imgSize}}" src="{{regions.valuesFile}}"></td><td><img width="{{imgSize}}" height="{{imgSize}}" src="{{regions.maskFile}}"></td><td><img width="{{imgSize}}" height="{{imgSize}}" src="{{regions.regionFile}}"></td><td><code>NO_OCEAN={{regions.noOceanInteriorOceanPixels}}/{{regions.noOceanInteriorPixels}}<br>ONLY_OCEAN={{regions.onlyOceanInteriorOceanPixels}}/{{regions.onlyOceanInteriorPixels}}</code></td></tr>
</table>
